<?xml version="1.0" encoding="utf-8"?>
<!--
  Một dòng trong thẻ "Trạng thái sạc": nhãn bên trái, giá trị bên phải, thanh tiến
  trình mảnh bên dưới. Dùng lại ba lần nên tách ra thành layout riêng.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvStatLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_body"
            tools:text="Dòng sạc" />

        <TextView
            android:id="@+id/tvStatValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_body"
            android:textStyle="bold"
            tools:text="3.5 W / 892 mA" />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/progressStat"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="@dimen/space_sm"
        android:max="100"
        android:progressDrawable="@drawable/bg_progress_track"
        tools:progress="55" />

</LinearLayout>
